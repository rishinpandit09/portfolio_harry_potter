{
  "compilerOptions": {
    // Target modern browsers with ESNext features
    "target": "ESNext",
    // Include standard library definitions for the DOM and ESNext
    "lib": ["DOM", "DOM.Iterable", "ESNext"],
    // Use native ES modules
    "module": "ESNext",
    "moduleResolution": "Node",
    // Enable the new React JSX transform (no need to import React for JSX)
    "jsx": "react-jsx",
    // Strict type‐checking options
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    // Allow default imports from CommonJS modules
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    // Skip type‐checking of declaration files for faster builds
    "skipLibCheck": true,
    // Support importing JSON files
    "resolveJsonModule": true,
    // Ensure each file is treated as an isolated module
    "isolatedModules": true,
    // Pull in React’s type definitions
    "types": ["react", "react-dom"]
  },
  // Point at your source files
  "include": ["src"]
}
