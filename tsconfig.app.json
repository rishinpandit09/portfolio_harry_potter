{
  "compilerOptions": {
    // Target modern browsers and enable class field support
    "target": "ESNext",
    "useDefineForClassFields": true,

    // Standard libs
    "lib": ["DOM", "DOM.Iterable", "ESNext"],

    // Skip checking types in all declaration files (faster builds)
    "skipLibCheck": true,

    // Module interop and default imports
    "esModuleInterop": false,
    "allowSyntheticDefaultImports": true,

    // Strict type-checking options
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "noFallthroughCasesInSwitch": true,

    // ES modules + JSON support
    "module": "ESNext",
    "moduleResolution": "node",
    "resolveJsonModule": true,
    "isolatedModules": true,

    // Enable the new JSX transform so you can drop `import React` just for JSX
    "jsx": "react-jsx",

    // Make sure React’s types are available
    "types": ["react", "react-dom"]
  },
  "include": ["src"]
}
